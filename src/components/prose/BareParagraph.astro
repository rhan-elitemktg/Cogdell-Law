---
// Portable Text `normal` block that emits a bare <p> — for band bodies whose
// paragraph styling comes from the band's own scoped `p` selector. Inline <em>
// and <strong> use astro-portabletext defaults.
---

<p><slot /></p>
